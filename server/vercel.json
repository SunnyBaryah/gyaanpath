{
  "version": 2,
  "builds": [
    {
      "src": "server/src/index.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["server/src/**/*.js"]
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/server/src/index.js" }]
}
